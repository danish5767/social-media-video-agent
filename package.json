{
  "name": "luma-social-companion",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "sync:android": "cap sync android",
    "open:android": "cap open android",
    "build:android": "cap sync android && cap build android"
  },
  "dependencies": {
    "@capacitor/android": "^7.4.0",
    "@capacitor/core": "^7.4.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.4.0"
  }
}
